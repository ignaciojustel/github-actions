name: Contexto
run-name: Aprendiendo a usar Contextos
on: push
jobs:
  check-main:
    if: ${{ github.ref == 'refs/heads/aprendiendo-github-actions' }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Desplegando en la rama $GITHUB_REF"
      - run: echo "El evento que desencadeno este job fue ${{github.event_name}}"
